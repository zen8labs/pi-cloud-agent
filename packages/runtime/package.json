{
  "name": "@pi-cloud-agent/runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runs inside the sandbox: checkout, one Pi session, outbound reporting",
  "main": "./run.ts",
  "exports": {
    ".": "./run.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsx build.ts",
    "template:build": "pnpm build && e2b template create pi-cloud-agent -c 'sleep infinity' --ready-cmd 'true' -d Dockerfile.sandbox --cpu-count 4 --memory-mb 4096"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "catalog:",
    "@pi-cloud-agent/protocol": "workspace:*",
    "pi-mcp-adapter": "2.21.2"
  },
  "devDependencies": {
    "@e2b/cli": "2.16.0",
    "@pi-cloud-agent/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "esbuild": "0.28.1",
    "typescript": "catalog:"
  }
}
