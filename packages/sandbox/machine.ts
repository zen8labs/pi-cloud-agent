/**
 * Machine size for every built-in sandbox provider.
 *
 * microSandbox applies this when creating a VM; E2B applies it when
 * materializing a template. Change these values to change both.
 */
export const SANDBOX_CPU_COUNT = 2;
export const SANDBOX_MEMORY_MB = 4096;
