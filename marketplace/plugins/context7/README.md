# context7

First-party demo plugin: skills + remote Context7 MCP.

Configure `CONTEXT7_API_KEY` in the dashboard after installing. The MCP server is
remote HTTP only (`https://mcp.context7.com/mcp`) — no local `npx` command.
